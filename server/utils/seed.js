/**
 * Initial incident dataset.
 *
 * Intentionally EMPTY — OpsPilot ships to production with no pre-seeded
 * incidents. The console starts as a clean slate; all incidents are created
 * by operators through the app.
 */
module.exports = [];
